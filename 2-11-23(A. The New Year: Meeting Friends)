Question : A. The New Year: Meeting Friends

#include<iostream>
#include <algorithm>
using namespace std;

int main()
{
    int arr[3];
    cin >> arr[0] >> arr[1] >> arr[2];
    int ans=0;
    sort(arr, arr+3);
    ans += (arr[1]-arr[0])+ (arr[2]-arr[1]);
    cout << ans << endl;
    return 0;
}
